{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-settings",
  "description": "Allows the bundled dashboard at the local daemon origin to trigger its narrow app-action bridge.",
  "local": false,
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:8765"]
  },
  "permissions": ["allow-run-tray-action"]
}
