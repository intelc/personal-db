"""App-owned browser connector bundles.

These files intentionally live outside the Chrome unpacked-extension root. The
native host passes only vetted bundle bytes to the stable extension runtime.
"""
