[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    live: tests that hit live external APIs (opt-in)
    darwin_only: tests that exercise a darwin-gated tracker or macOS-only machinery (launchd, Full Disk Access paths, etc.) and cannot run on non-macOS CI runners
addopts = -ra --strict-markers -m "not live"
